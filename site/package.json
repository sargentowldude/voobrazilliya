{
  "name": "tema-prazdniki-kemerovo",
  "version": "1.0.0",
  "private": true,
  "description": "Сайт студии детских праздников в Кемерово",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/admin.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.5"
  }
}
